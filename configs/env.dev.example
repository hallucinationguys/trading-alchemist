# Development Environment Configuration

# Server Configuration
SERVER_HOST=localhost
SERVER_PORT=8080
SERVER_READ_TIMEOUT=10s
SERVER_WRITE_TIMEOUT=10s

# Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=trading_alchemist_db
DB_SSL_MODE=disable
DB_MAX_CONNS=25
DB_MIN_CONNS=5
DB_MAX_CONN_LIFE=1h
DB_MAX_CONN_IDLE=30m

# JWT Configuration
JWT_SECRET=dev-secret-key-change-this-in-production
JWT_TTL=24h

# Email Configuration (Resend only)
RESEND_API_KEY=re_your_real_api_key_here
FROM_EMAIL=noreply@hallucinationguys.com
FROM_NAME=Trading Alchemist (Dev)

# Application Configuration
APP_NAME=Trading Alchemist
APP_ENV=development
APP_BASE_URL=http://localhost:8080
MAGIC_LINK_TTL=15m 