# Test Environment Configuration

# Server Configuration
SERVER_HOST=localhost
SERVER_PORT=8081
SERVER_READ_TIMEOUT=5s
SERVER_WRITE_TIMEOUT=5s

# Database Configuration
DB_HOST=localhost
DB_PORT=5434
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=trading_alchemist_test_db
DB_SSL_MODE=disable
DB_MAX_CONNS=10
DB_MIN_CONNS=2
DB_MAX_CONN_LIFE=30m
DB_MAX_CONN_IDLE=10m

# JWT Configuration
JWT_SECRET=test-secret-key-for-testing-only
JWT_TTL=1h

# Email Configuration (Resend only)
RESEND_API_KEY=re_test_xxxxxxxxx
FROM_EMAIL=test@localhost.test
FROM_NAME=Trading Alchemist (Test)

# Application Configuration
APP_NAME=Trading Alchemist
APP_ENV=test
APP_BASE_URL=http://localhost:8081
FRONTEND_BASE_URL=http://localhost:3000
MAGIC_LINK_TTL=5m
DEFAULT_MODEL=openai/gpt-4o-mini
ENCRYPTION_KEY=a-test-secret-key-that-is-32-bytes 